require 'spree/core'

namespace :deploy do
  task :seed do
    on roles(:db) do
      load_defaults = Spree::Country.count == 0
      if load_defaults
        ENV['AUTO_ACCEPT'] = true
        execute :rake, 'db:seed'
      end
    end
  end
end
